# 鸣鹤 (MingHe) 服务器配置

[server]
# 监听地址
listen_addr = "0.0.0.0"
# SIP TLS 端口 (SIPS)
sip_port = 5061
# 服务器域名或 IP 地址
# 用于 SIP URI 和 TLS 证书生成
# 可以是域名（如 "minghe.local"）或 IP 地址（如 "192.168.1.100"）
host = "minghe.local"

[extensions]
# 分机号码范围
range_start = 1000
range_end = 2000
# 所有分机的默认密码（未在 [passwords] 中单独配置的分机使用此密码）
default_password = "minghe@2024"

[tls]
# TLS 最低协议版本（"1.0"/"1.1"/"1.2"/"1.3"，默认 "1.2"）
# 接入 HX4G 等老式网关时，若其只支持 TLS 1.0/1.1，可下调（降低安全性，仅限受信网络）
tls_min_version = "1.2"
# 证书路径（留空则自动生成自签名证书）
# 支持域名证书和 IP 证书
cert_path = ""
key_path = ""

[media]
# RTP 中继端口范围
rtp_port_start = 20000
rtp_port_end = 20020
# 服务器公网/内网 IP（用于 SDP 中的媒体地址）
# 不要留空。请替换为 Bria、Linkvil 等客户端能访问到的公网或内网 IP。
# Docker、云平台或多网卡环境下自动检测通常会拿到容器内网 IP，导致接听后无声。
media_addr = "CHANGE_ME_MEDIA_IP"

# 单独为分机设置密码（覆盖 default_password）
# 格式：分机号 = "密码"
# 未列出的分机使用 default_password
[passwords]
# 1001 = "velox@2026"
# 1002 = "alice@2026"
# 1003 = "bob@2026"
